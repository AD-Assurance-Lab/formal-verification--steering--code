# Archived 2026-09-01

Moved to the portable drive, verified by sha256, and removed from the repo:

    <drive>/ad-assurance-archive/formal-verification--steering--code/2026-09-01/

| archive | size | what it is |
|---|---|---|
| `results_town04_v2__pre_2988_cut.tar.zst` | 261 MB | Town04 redo captures + ledger at the 2,861 m scored prefix, superseded when the redo moved to 2,988 m |
| `results_town06__superseded_six_sections.tar.zst` | 16 KB | Town06 results from the six-section era, superseded by the single continuous lap |
| `results_town06_logs__six_sections.tar.zst` | 4 KB | run logs for the above |

The drive's `README.md` carries the full manifest and the reason each was superseded.
`SHA256SUMS` beside them verifies the restore. Marked for deletion; no deadline.

These are recoverable twice over: from the drive, and from this repo's git history
before this commit. Nothing in the repo reads them and the audit excludes them by path.
