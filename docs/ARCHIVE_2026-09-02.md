# Archived 2026-09-02

Moved to the portable drive and removed from the repo:

    <drive>/ad-assurance-archive/formal-verification--steering--code/2026-09-02/

| archive | size | what it is |
|---|---|---|
| `data/clear_t06lap`, `data/mixed_t06lap` | 3.9 GB | Town06 lap BC datasets, both collected under the darkened render (T06-F42) |
| `data/dagger_clear_t06lap`, `data/dagger_mixed_t06lap` | 16 GB | Town06 lap DAgger sets, each holding BOTH renderings interleaved |
| `checkpoints/teacher_*_t06lap_*.pth` | 21 files | every lap teacher trained on the above |
| `data/_scratch_render_check` | 261 MB | the 2026-09-02 collection that established the correct value; evidence, not data |

**Why:** `docs/TOWN06_FINDINGS.md` T06-F42. The server rendered the identical scene ~15%
darker for part of 2026-09-01; both teachers trained on frames darker than the frames they
are scored on. PROTOCOL A-2 established that data captured under a superseded rendering is
recollected rather than re-evaluated.

The drive's `README.md` in that directory records which rounds fell on which side.
Recoverable from the drive and from this repo's git history before this commit. Nothing in
the repo reads these paths. Marked for deletion; no deadline.

The six-section era (2026-08-28) is unaffected by this defect and is superseded for a
different reason: the route.

## Second archive, same day: the wild-label era

    <drive>/.../2026-09-02/wild_labels/

| archive | what it is |
|---|---|
| `data/clear_t06lap`, `data/mixed_t06lap` | BC sets collected 07:16-07:27 on the corrected RENDER, discarded for a different defect |
| `data/dagger_clear_t06lap` | the clear teacher's 4 DAgger rounds |
| `checkpoints/teacher_clear_t06lap_*` | the clear teacher, which passed its gate 3/3 at 44-56% of budget |

**Why:** `docs/TOWN06_FINDINGS.md` T06-F43. Every lap ran a few steps past the end of the
open route, where pure pursuit's lookahead clamps onto the final vertex and the expert
LABEL degenerates -- 13 frames of 15,360 at |steer| up to 0.754 against a lap maximum of
0.086, all at the end of the scored road. Collected before `route.lap_finished()` existed.

These were good data in every other respect: the render is correct (0.2526-0.2534), the
speed agreement is 1.00, the coverage is full. They are superseded for the labels alone.
